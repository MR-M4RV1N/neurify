{% extends 'cpanel/_layouts/app.html.twig' %}

{% block title %}{% trans %}New achievement{% endtrans %}{% endblock %}

{% block body %}
    <div class="main-content-container container-fluid">
        <!-- HEADER... -->
        <div class="page-header row no-gutters py-4">
            <div class="col-12 col-sm-4 text-center text-sm-left mb-0">
                <h3 class="page-title mb-2">MBTI</h3>
                <span class="text-uppercase page-subtitle">
                    {% trans %}Your personality type{% endtrans %}
                </span>
            </div>
        </div>
        <!-- ...HEADER -->
        <!-- CONTENT... -->
        <div class="row border bg-white p-2">
            <a href="{{ path('ai_consulting')}}" class="btn btn-sm btn-outline-secondary"><i class="fas fa-angle-left"></i> {% trans %}back{% endtrans %}</a>
        </div>
        <div class="row border bg-white mb-5">
            <div class="col-lg-12 col-md-12 col-sm-12 mt-4 mb-4">
                {% if mbti and mbti.updatedAt %}
                    <p class="text-muted text-right">
                        {% trans %}Обновлено{% endtrans %}: {{ mbti.updatedAt|date('Y-m-d H:i') }}
                    </p>
                {% endif %}

                <div>
                    <p>{% trans %}mbti about{% endtrans %}</p>
                    <p>{% trans %}The test can be taken here{% endtrans %}: <a href="https://www.16personalities.com/">16personalities</a></p>
                </div>

                {{ form_start(form) }}
                {{ form_widget(form.content, {'attr': {
                    'class': 'form-control',
                    'rows': 10,
                    'data-ckeditor': 'true',
                    'placeholder': 'Describe your resource map…'
                }}) }}
                {{ form_errors(form.content) }}

                <div class="text-center mt-2">
                    {{ form_row(form.save) }}
                </div>
                {{ form_end(form) }}
            </div>
        </div>
        <!-- ...CONTENT -->
    </div>
{% endblock %}