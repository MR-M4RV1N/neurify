{% extends 'cpanel/_layouts/app.html.twig' %}

{% block title %}Edit Blank{% endblock %}

{% block body %}
    <div class="main-content-container container-fluid">
        <div class="row">
            <div class="col">
                <div class="card card-small p-1 mt-4">
                    <div class="card-header border-bottom">
                        <h6 class="m-0 text-center">{% trans %}Edit item{% endtrans %}</h6>
                    </div>
                    <div class="card-body">
                        {{ include('cpanel/blank/_form.html.twig', {'button_label': 'Update'}) }}

                        <div class="text-right">
                            {{ include('cpanel/blank/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
