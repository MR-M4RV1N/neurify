{% extends 'cpanel/_layouts/app.html.twig' %}

{% block title %}Ensemble{% endblock %}

{% block body %}
{#    <div class="container-fluid bg-white border">#}
{#        <div class="border-bottom">#}
{#            <h6 class="m-0 text-center">#}
{#                {% trans %}Group{% endtrans %} "{{ ensemble.title }}"#}
{#            </h6>#}
{#        </div>#}
{#        <div class="mt-3">#}
{#            <p class="text-justify">{{ ensemble.description }}</p>#}
{#        </div>#}
{#        <div class="d-flex justify-content-end">#}
{#            {% if ensemble.author.id == app.user.id %}#}
{#                <a href="{{ path('app_ensemble_edit', {'id': ensemble.id}) }}" class="btn sm btn-outline-primary"><i class="material-icons">edit</i> {% trans %}Edit{% endtrans %}</a>#}
{#            {% endif %}#}
{#        </div>#}
{#    </div>#}
    <div class="main-content-container container-fluid">
        <!-- Page Header -->
        <div class="page-header row no-gutters pb-3 pt-3">
            <div class="col-12 col-sm-4 text-center text-sm-left mb-0">
                <!--<span class="text-uppercase page-subtitle">{% trans %}All{% endtrans %}</span>-->
                <h3 class="page-title mb-2">{{ ensemble.title }}</h3>
                <div class="mt-3">
                    <p class="text-justify">{{ ensemble.description }}</p>
                </div>
                <div class="d-flex justify-content-center mb-3">
                    {% if ensemble.author.id == app.user.id %}
                        <a href="{{ path('app_ensemble_edit', {'id': ensemble.id}) }}" class="btn btn-sm btn-primary"><i class="material-icons">edit</i> {% trans %}Edit{% endtrans %}</a>
                    {% endif %}
                </div>
            </div>
        </div>

{#        <div class="d-flex justify-content-center mb-4">#}
{#            <div class="devstyle-circle-button" onclick="location.href='{{ path('app_event_new') }}'">#}
{#                <i class="material-icons">add</i>#}
{#            </div>#}
{#        </div>#}

        {% include '/cpanel/events/_common/_iteration.html.twig' %}
    </div>

    <!-- ADD FLOATING BUTTON -->
    <div class="devstyle-floating-button" onclick="location.href='{{ path('app_event_new_in_ensemble', {'id': ensemble.id}) }}'">+</div>
{% endblock %}

