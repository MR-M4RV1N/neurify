{% extends 'cpanel/_layouts/app.html.twig' %}

{% block title %}Classification
{% endblock %}

{% block body %}
	{% include '/cpanel/events/user/_profile.html.twig' %}

	{% include '/cpanel/events/user/_tabs.html.twig' %}

	<div class="container text-center mb-3">
		<form method="get" action="{{ path('app_users_classification', { id: profile.id }) }}" class="d-inline-block">
			<select name="year" class="form-control form-control-sm d-inline-block w-auto" onchange="this.form.submit()">
				{% for y in range(currentYear, currentYear - 6) %}
					<option value="{{ y }}" {{ y == selectedYear ? 'selected' : '' }}>
						{{ y }}
					</option>
				{% endfor %}
			</select>
		</form>
	</div>

	<div class="main-content-container container-fluid px-4">
		<div class="row mt-4">
			<div class="col-12">
				{% include '/cpanel/events/user/_classification.html.twig' %}
			</div>
		</div>
	</div>
	<!-- Spacer -->
	<div class="my-5"></div>
{% endblock %}
